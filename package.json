{
  "name": "seed-ts",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript implementation of the seed CLI/runner spec — see registry/seed-spec/seed-spec.md v0.4.1.",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "bun --bun tsc -b --noEmit",
    "test": "bun test",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.0"
  },
  "engines": {
    "bun": ">=1.1.0",
    "node": ">=20"
  }
}
